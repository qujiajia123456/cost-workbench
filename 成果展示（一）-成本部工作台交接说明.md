# 成果展示（一）：成本部工作台

## 1. 项目定位

本项目是烟建集团成本部的本地网页工作台，服务于基层单位报表上传、自动汇总、Excel/Word 成果导出，以及后续成本管理应用的逐步接入。

当前主页地址：`http://127.0.0.1:8899/`

主要入口：

1. 成本部工作台：首页，展示企业文化图片及应用建设进展。
2. 竣工结算报表：上传各分公司竣工结算定案报表，自动校验、汇总、导出成果包。
3. PM平台红蓝预警：预留 PM 数据接入与预警展示。
4. 材料采购价格比对：材料价格信息的导入、比对与维护。
5. 分包价格库：分供、分包价格库维护。
6. 黑名单关联查询：企业关联线索查询。
7. 围标串标识别：预留投标异常识别入口。
8. 上传记录、报表接口：分别用于追溯上传情况和扩展新的报表类型。

## 2. 技术结构

| 位置 | 作用 |
| --- | --- |
| `server.py` | 后端、Excel 解析、汇总规则、Excel/Word 导出、接口处理的主文件。 |
| `static/index.html` | 页面结构和左侧导航。 |
| `static/app.js` | 网页交互、上传、汇总、导出和页面切换。 |
| `static/styles.css` | 工作台视觉样式和自适应布局。 |
| `templates/` | Excel、Word 正式模板。修改报表外观时优先维护这里。 |
| `data/` | 本机运行数据、上传记录、台账和导出结果；不得提交到版本库。 |
| `tools/` | PM、材料价格、黑名单等辅助脚本。 |

运行环境为 Python，本机启动端口为 `8899`。启动工作台时不得自动打开“烟建PM平台（第四版）”或“大项目管理平台”；确需抓取 PM 数据时，应先在网页中提示用户打开相应平台。

## 3. 竣工结算报表

### 3.1 上传与识别

- 支持 `.xlsx` 与 `.xls` 批量上传。
- 分公司名称优先从报表内容识别，文件名仅作为辅助。
- 汇总顺序按固定分公司顺序，不能简单按上传先后或字母排序。
- 一键清空需同时清除当前汇总数据和上传记录；上传记录页面的一键清空清除全部上传记录。
- 发生格式异常、公司未上传、日期无法识别、辅助标记和日期冲突时，应记录到问题反馈中，不得静默篡改数据。

### 3.2 核心口径

1. A：本年度已定案工程。
2. B：本年度 6 月 30 日及以前实际竣工、截至本月仍未定案工程。
3. C：本年度 7 月 1 日及以后实际竣工、截至本月仍未定案工程。
4. B、C 均以明细行“实际竣工时间”为准，不能只信源表分段标题。
5. 在建、未开工、停工、诉讼等非统计工程不纳入未定案/严重逾期统计；但已经定案的诉讼项目仍应纳入已定案统计。
6. 本月新增定案以“结算定案时间”按闭区间判断。例如 2026 年 7 月统计期为 `2026-06-21` 至 `2026-07-20`。
7. 若定案时间在上期、但上期因源表未取回而未曾统计，本期可补统计；补统计必须有可追溯依据，不能仅由辅助列标记决定。
8. 所有合同额按真实数值计算并保留两位小数，不按显示整数倒推。

详细取数规则见：`竣工结算报表取数规则-待确认.md`。

### 3.3 导出成果

点击“导出成果包”后生成 ZIP：

| 成果 | 形式 |
| --- | --- |
| 01 竣工及未定案工程 | Excel 工作表 |
| 02 各单位竣工结算定案工程一览表 | Excel 工作表 |
| 03 建安主业单位项目竣工结算定案情况 | Excel 工作表 |
| 04 季度严重逾期工程明细表 | Excel 工作表 |
| 关于竣工结算完成情况的通报 | Word 文件 |
| 关于竣工结算递交情况的考核 | Word 文件 |
| 关于季度定案严重逾期工程统计情况的通报 | Word 文件 |

Excel 页签会重新编号，通知类报表不再作为 Excel 页签保留。

### 3.4 Word 通知母版

以下三个文件是 Word 正式母版，导出时必须直接以母版为基础填充，不得通过重新绘制表格替代：

1. `templates/settlement-notice-02.docx`
2. `templates/settlement-notice-03.docx`
3. `templates/settlement-notice-06.docx`

导出时仅替换标题、正文、表格数据、落款和日期；页面尺寸、页边距、字体、字号、颜色、行距、边框、列宽和合并单元格保持母版设置。

02 通报的“定案率”必须显示为百分数并保留一位小数，例如 `50.0%`，Excel 与 Word 输出均应一致。

### 3.5 05 表样式规则

工作表名称示例：`05建安主业单位2026年7月项目竣工结算定案情况`。

- D 列“合同额（万元）”采用真实数值计算，显示保留两位小数。
- 目标定案率显示百分数。
- 负责人范围可着浅蓝色数据区，但 A、B 列“督导领导、督导单位”不得随行填充。
- 集团总计行浅绿色。
- 计划数量来源于上月分公司报表；本月实际完成来源于本月新增定案项目；下月计划从本月分公司报表中提取。

### 3.6 06、07 表季度规则

- 06 表“上季度”不能临时手填后遗失，应通过每月严重逾期统计台账逐月累计形成季度数。
- 07 表标题、工作表名称和统计区间均应由所选月份自动确定。例如 7 月导表对应三季度，且标题使用“三季度”。
- 严重逾期工程的统计应排除“/”、诉讼、在建、未开工、停工等非统计明细。

## 4. 当前版本状态

- 本地 Git 分支：`feature/word-notice-templates`
- 当前提交：`a03ba7b`
- 本地版本已包含 Word 母版导出和定案率百分数一位小数的修改。
- GitHub 远程仓库尚未配置；后续需提供 GitHub 仓库 HTTPS 地址后执行推送。

## 5. 后续开发原则

1. 先核对源表明细，再修改统计口径；禁止仅为使数字“看起来一致”而调整逻辑。
2. 所有新增统计规则应在问题反馈中体现其来源、条件和排除原因。
3. 修改 Excel/Word 格式时，优先使用已确认的人工样表作为模板。
4. 对涉及月份的规则，使用实际日期区间动态计算，不在代码中写死“6 月 20 日”等日期。
5. 新增报表建议实现为独立 `ReportPlugin`，复用上传、校验、汇总、问题反馈和导出框架。
6. 提交 Git 时，只提交代码、模板和说明；不得提交 `data/` 中的上传文件、数据库、业务台账及导出结果。

## 6. 交接检查清单

- [ ] 能打开 `http://127.0.0.1:8899/`。
- [ ] 可批量上传 `.xls/.xlsx` 分公司报表。
- [ ] 可在问题反馈中看到缺失、异常或冲突信息。
- [ ] 可导出包含 Excel 和三份 Word 的成果包。
- [ ] 三份 Word 的页面与正式母版一致，定案率显示一位小数百分数。
- [ ] 清空当前数据和清空上传记录均符合页面文案。
- [ ] 未自动打开 PM 平台；仅在确需 PM 数据时提示用户操作。
